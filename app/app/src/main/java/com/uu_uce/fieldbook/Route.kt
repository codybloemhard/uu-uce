package com.uu_uce.fieldbook

// TODO: implement route
/*import android.graphics.Canvas
import android.graphics.Color
import android.graphics.Paint
import android.view.View
import com.uu_uce.services.UTMCoordinate
import com.uu_uce.shapefiles.p2
import java.time.LocalDate
import java.time.LocalTime

typealias RoutePoint = Pair<UTMCoordinate, LocalTime>

class Route (
    private val id          : Int,
    private val route       : FullRoute,
    private val date        : LocalDate
    //private val viewModel   : RouteViewModel
) {
    private val paint = Paint(Color.GREEN).apply {
        this.style = Paint.Style.STROKE
        this.strokeWidth = 4f
    }

    fun draw(viewport: Pair<p2, p2>, view: View, canvas: Canvas) {
        canvas.drawLines(
            route.drawLines(viewport,view),
            paint
        )
    }
}*/

/* This program has been developed by students from the bachelor Computer
# Science at Utrecht University within the Software Project course. ©️ Copyright
# Utrecht University (Department of Information and Computing Sciences)*/

